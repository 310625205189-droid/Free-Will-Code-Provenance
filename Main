<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IET Connect | Vibrant & Functional</title>
    <style>
        /* VIBRANT DESIGN SYSTEM */
        :root {
            --primary: #622569; /* IET Deep Purple */
            --primary-light: #8e44ad;
            --accent: #9b51e0;
            --bg-gradient: linear-gradient(135deg, #622569 0%, #2c3e50 100%);
            --glass: rgba(255, 255, 255, 0.95);
            --text: #2d3436;
            --white: #ffffff;
            --transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background: #f0f2f5;
            color: var(--text);
            overflow-x: hidden;
        }

        /* ANIMATIONS */
        @keyframes slideUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        /* AUTH SCREENS (LOGIN & REGISTER) */
        .auth-wrapper {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: var(--bg-gradient);
            padding: 20px;
        }

        .auth-card {
            background: var(--glass);
            padding: 40px;
            border-radius: 20px;
            width: 100%;
            max-width: 400px;
            box-shadow: 0 15px 35px rgba(0,0,0,0.2);
            text-align: center;
            animation: slideUp 0.6s ease-out;
            display: none; /* Controlled by JS */
        }

        .auth-card.active { display: block; }

        .logo-img {
            width: 180px;
            margin-bottom: 20px;
        }

        /* FORM ELEMENTS */
        .form-group {
            text-align: left;
            margin-bottom: 15px;
        }

        label {
            display: block;
            font-size: 0.85rem;
            font-weight: 600;
            margin-bottom: 5px;
            color: var(--primary);
        }

        input {
            width: 100%;
            padding: 12px 15px;
            border: 2px solid #eee;
            border-radius: 10px;
            outline: none;
            transition: var(--transition);
        }

        input:focus {
            border-color: var(--primary);
            box-shadow: 0 0 10px rgba(98, 37, 105, 0.1);
        }

        .vibrant-btn {
            width: 100%;
            padding: 12px;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 10px;
            font-weight: bold;
            cursor: pointer;
            transition: var(--transition);
            margin-top: 10px;
        }

        .vibrant-btn:hover {
            background: var(--accent);
            transform: scale(1.02);
        }

        .toggle-link {
            margin-top: 15px;
            font-size: 0.9rem;
            cursor: pointer;
            color: var(--primary);
            text-decoration: underline;
        }

        /* MAIN APP LAYOUT */
        #app-shell {
            display: none;
            height: 100vh;
            display: grid;
            grid-template-columns: 260px 1fr;
        }

        /* SIDEBAR */
        aside {
            background: var(--primary);
            color: white;
            padding: 30px 0;
            display: flex;
            flex-direction: column;
        }

        .nav-item {
            padding: 15px 25px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 10px;
            font-weight: 500;
        }

        .nav-item:hover, .nav-item.active {
            background: rgba(255,255,255,0.1);
            padding-left: 35px;
            border-left: 4px solid var(--white);
        }

        /* MAIN CONTENT */
        main {
            padding: 40px;
            overflow-y: auto;
            background: #f8f9fa;
        }

        .page-content {
            display: none;
            animation: fadeIn 0.5s ease-in;
        }

        .page-content.active { display: block; }

        /* PROFILE PAGE SPECIAL DESIGN */
        .profile-header {
            display: flex;
            align-items: center;
            gap: 20px;
            margin-bottom: 30px;
            background: var(--white);
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        .avatar {
            width: 100px;
            height: 100px;
            background: var(--accent);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: white;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-top: 20px;
        }

        .stat-card {
            background: var(--white);
            padding: 20px;
            border-radius: 15px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
        }

        .stat-card h3 { color: var(--primary); font-size: 1.5rem; }

    </style>
</head>
<body>

    <!-- AUTHENTICATION SECTION -->
    <div id="auth-section" class="auth-wrapper">
        
        <!-- LOGIN CARD -->
        <div id="login-card" class="auth-card active">
            <!-- Replace 'logo.png' with your actual image path -->
            <img src="https://upload.wikimedia.org/wikipedia/en/thumb/0/0f/IET_Logo.svg/1200px-IET_Logo.svg.png" alt="IET Logo" class="logo-img">
            <h2>Welcome Back</h2>
            <p style="margin-bottom: 20px; color: #666;">Sign in to IET Connect</p>
            
            <div class="form-group">
                <label>Email Address</label>
                <input type="email" placeholder="name@domain.com" id="login-email">
            </div>
            <div class="form-group">
                <label>Password</label>
                <input type="password" placeholder="••••••••">
            </div>
            <button class="vibrant-btn" onclick="performLogin()">Sign In</button>
            <p class="toggle-link" onclick="toggleAuth('register-card')">Need an account? Register here</p>
        </div>

        <!-- REGISTRATION CARD -->
        <div id="register-card" class="auth-card">
            <h2>Join IET Connect</h2>
            <p style="margin-bottom: 20px; color: #666;">Create your member account</p>
            
            <div class="form-group">
                <label>Full Name</label>
                <input type="text" placeholder="John Doe">
            </div>
            <div class="form-group">
                <label>Email Address</label>
                <input type="email" placeholder="name@domain.com">
            </div>
            <div class="form-group">
                <label>Membership Type</label>
                <select style="width:100%; padding: 12px; border-radius: 10px; border: 2px solid #eee;">
                    <option>Student Member</option>
                    <option>Professional Member</option>
                    <option>Fellow</option>
                </select>
            </div>
            <div class="form-group">
                <label>Password</label>
                <input type="password" placeholder="Create Password">
            </div>
            <button class="vibrant-btn" onclick="performLogin()">Create Account</button>
            <p class="toggle-link" onclick="toggleAuth('login-card')">Already have an account? Login</p>
        </div>
    </div>

    <!-- MAIN APP SECTION -->
    <div id="app-shell" style="display: none;">
        <aside>
            <div style="padding: 0 25px 30px; font-weight: bold; font-size: 1.4rem;">IET CONNECT</div>
            
            <div class="nav-item active" onclick="showPage('dash-page', this)">🏠 Dashboard</div>
            <div class="nav-item" onclick="showPage('events-page', this)">📅 Events</div>
            <div class="nav-item" onclick="showPage('projects-page', this)">🚀 Projects</div>
            <div class="nav-item" onclick="showPage('profile-page', this)">👤 My Profile</div>
            
            <div style="margin-top: auto;" class="nav-item" onclick="logout()">🚪 Logout</div>
        </aside>

        <main>
            <!-- DASHBOARD -->
            <section id="dash-page" class="page-content active">
                <h1 style="margin-bottom: 20px;">Welcome back, Member!</h1>
                <div class="stats-grid">
                    <div class="stat-card"><h3>12</h3><p>Upcoming Events</p></div>
                    <div class="stat-card"><h3>4</h3><p>Active Projects</p></div>
                    <div class="stat-card"><h3>85%</h3><p>Profile Strength</p></div>
                </div>
            </section>

            <!-- PROFILE PAGE -->
            <section id="profile-page" class="page-content">
                <h1 style="margin-bottom: 20px;">My Profile</h1>
                
                <div class="profile-header">
                    <div class="avatar">JD</div>
                    <div>
                        <h2>John Doe</h2>
                        <p style="color: var(--primary-light)">MIET - Professional Member</p>
                        <p style="font-size: 0.8rem; color: #888;">Member since 2023</p>
                    </div>
                    <button class="vibrant-btn" style="width: auto; margin-left: auto; padding: 10px 25px;">Edit Profile</button>
                </div>

                <div style="background: white; padding: 30px; border-radius: 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.05);">
                    <h3 style="margin-bottom: 20px; border-bottom: 2px solid #f0f2f5; padding-bottom: 10px;">Account Details</h3>
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
                        <div class="form-group">
                            <label>Email</label>
                            <input type="text" value="john.doe@iet.org" disabled>
                        </div>
                        <div class="form-group">
                            <label>Phone</label>
                            <input type="text" value="+44 20 1234 5678">
                        </div>
                        <div class="form-group">
                            <label>Chapter</label>
                            <input type="text" value="London Central">
                        </div>
                        <div class="form-group">
                            <label>Engineering Field</label>
                            <input type="text" value="Software Engineering">
                        </div>
                    </div>
                </div>
            </section>

            <!-- Placeholder sections for others -->
            <section id="events-page" class="page-content"><h1>Events</h1><p>Vibrant events list coming soon...</p></section>
            <section id="projects-page" class="page-content"><h1>Projects</h1><p>Vibrant project collaborations coming soon...</p></section>

        </main>
    </div>

    <script>
        // TOGGLE BETWEEN LOGIN AND REGISTER
        function toggleAuth(cardId) {
            document.querySelectorAll('.auth-card').forEach(card => card.classList.remove('active'));
            document.getElementById(cardId).classList.add('active');
        }

        // SIMULATE LOGIN
        function performLogin() {
            const authSection = document.getElementById('auth-section');
            const appShell = document.getElementById('app-shell');

            authSection.style.transition = "0.5s";
            authSection.style.opacity = "0";
            
            setTimeout(() => {
                authSection.style.display = "none";
                appShell.style.display = "grid";
                appShell.style.animation = "fadeIn 0.8s ease-in";
            }, 500);
        }

        function logout() {
            location.reload(); // Simple way to reset state
        }

        // PAGE NAVIGATION
        function showPage(pageId, navEl) {
            // Update Sidebar
            document.querySelectorAll('.nav-item').forEach(el => el.classList.remove('active'));
            navEl.classList.add('active');

            // Update Content
            document.querySelectorAll('.page-content').forEach(p => p.classList.remove('active'));
            const targetPage = document.getElementById(pageId);
            targetPage.classList.add('active');
        }
    </script>
</body>
</html>
