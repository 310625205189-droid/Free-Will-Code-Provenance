<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IET CONNECT | Member & Admin Portal</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #622569; 
            --accent: #9b51e0;
            --danger: #ff4757; /* Vibrant Red for warnings */
            --bg: #f4f7f6;
            --white: #ffffff;
            --text: #2d3436;
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; font-family: 'Poppins', sans-serif; }
        body { background: var(--bg); color: var(--text); overflow: hidden; }

        /* ANIMATIONS */
        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
        @keyframes slideIn { from { transform: translateX(-100%); } to { transform: translateX(0); } }
        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-8px); }
            75% { transform: translateX(8px); }
        }

        .shake { animation: shake 0.2s ease-in-out 0s 2; }

        /* AUTHENTICATION LAYOUT */
        .auth-container {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, var(--primary) 0%, #2c3e50 100%);
        }

        .auth-card {
            background: var(--white);
            padding: 40px;
            border-radius: 20px;
            width: 100%;
            max-width: 400px;
            box-shadow: 0 20px 40px rgba(0,0,0,0.3);
            text-align: center;
            animation: fadeIn 0.6s ease-out;
            position: relative;
        }

        /* WARNING MESSAGE BOX */
        .error-box {
            display: none;
            background: #ffe0e3;
            color: var(--danger);
            border: 1px solid var(--danger);
            padding: 10px;
            border-radius: 10px;
            font-size: 0.8rem;
            margin-bottom: 20px;
            font-weight: 600;
            animation: fadeIn 0.3s ease-in;
        }

        .logo-img { width: 180px; margin-bottom: 20px; }
        
        .form-group { text-align: left; margin-bottom: 15px; }
        label { display: block; font-size: 0.8rem; font-weight: 600; margin-bottom: 5px; color: var(--primary); }
        input { 
            width: 100%; padding: 12px; border: 2px solid #eee; border-radius: 10px; 
            transition: var(--transition); outline: none;
        }
        input:focus { border-color: var(--primary); }

        .btn {
            width: 100%; padding: 12px; background: var(--primary); color: white;
            border: none; border-radius: 10px; font-weight: 600; cursor: pointer;
            transition: var(--transition); margin-top: 10px;
        }
        .btn:hover { background: var(--accent); transform: translateY(-2px); }

        .toggle-link { margin-top: 15px; font-size: 0.85rem; color: var(--primary); cursor: pointer; text-decoration: underline; }

        /* MAIN APP LAYOUT (Hidden by Default) */
        #app-shell { display: none; height: 100vh; grid-template-columns: 260px 1fr; }

        aside {
            background: var(--primary); color: white; padding: 30px 0;
            display: flex; flex-direction: column; animation: slideIn 0.5s ease-out;
        }

        .sidebar-header { padding: 0 25px 30px; font-size: 1.2rem; font-weight: 600; letter-spacing: 1px; }

        .nav-item {
            padding: 15px 25px; cursor: pointer; transition: var(--transition);
            display: flex; align-items: center; gap: 12px; opacity: 0.8;
        }
        .nav-item:hover, .nav-item.active {
            opacity: 1; background: rgba(255,255,255,0.1);
            border-left: 5px solid white; padding-left: 30px;
        }

        main { padding: 40px; overflow-y: auto; background: #f9f9f9; }
        .page { display: none; animation: fadeIn 0.4s ease-out; }
        .page.active { display: block; }

        .role-badge {
            background: rgba(255,255,255,0.2); padding: 4px 10px; border-radius: 20px;
            font-size: 0.7rem; text-transform: uppercase; margin-top: 5px; display: inline-block;
        }
    </style>
</head>
<body>

    <!-- AUTH SECTION -->
    <div id="auth-section" class="auth-container">
        
        <div id="login-card" class="auth-card">
            <img src="https://upload.wikimedia.org/wikipedia/en/thumb/0/0f/IET_Logo.svg/1200px-IET_Logo.svg.png" class="logo-img">
            
            <!-- Error Message Div -->
            <div id="login-error" class="error-box">⚠️ Invalid Email or Password</div>

            <h2 style="margin-bottom: 20px;">Welcome Back</h2>
            <div class="form-group">
                <label>Email Address</label>
                <input type="email" id="login-email" placeholder="name@domain.com" oninput="hideError()">
            </div>
            <div class="form-group">
                <label>Password</label>
                <input type="password" id="login-pass" placeholder="••••••••" oninput="hideError()">
            </div>
            <button class="btn" onclick="auth.login()">Sign In</button>
            <p class="toggle-link" onclick="toggleAuth('register-card')">New here? Create an account</p>
        </div>

        <div id="register-card" class="auth-card" style="display:none;">
            <h2 style="margin-bottom: 20px;">Join IET Connect</h2>
            <div class="form-group">
                <label>Full Name</label>
                <input type="text" id="reg-name" placeholder="John Doe">
            </div>
            <div class="form-group">
                <label>Email Address</label>
                <input type="email" id="reg-email" placeholder="name@domain.com">
            </div>
            <div class="form-group">
                <label>Password</label>
                <input type="password" id="reg-pass" placeholder="••••••••">
            </div>
            <button class="btn" onclick="auth.register()">Create Member Account</button>
            <p class="toggle-link" onclick="toggleAuth('login-card')">Already have an account? Login</p>
        </div>
    </div>

    <!-- APP SHELL -->
    <div id="app-shell">
        <aside>
            <div class="sidebar-header">
                IET CONNECT
                <div id="user-role-badge" class="role-badge">Member</div>
            </div>
            <div id="dynamic-menu"></div>
            <div class="nav-item" onclick="auth.logout()" style="margin-top: auto; color: #ff7675;">Logout</div>
        </aside>

        <main>
            <div id="dash-page" class="page active">
                <h1 id="welcome-msg">Dashboard</h1>
                <p>Welcome to your portal.</p>
            </div>
            <div id="events-page" class="page"><h1>Events</h1><p>Event list loading...</p></div>
            <div id="admin-members-page" class="page"><h1>Member Management</h1><p>Admin tools active.</p></div>
            <!-- Additional pages can be added here -->
        </main>
    </div>

    <script>
        // INIT MOCK DB
        if (!localStorage.getItem('users')) {
            const initialUsers = [{ name: "System Admin", email: "admin@iet.com", pass: "admin123", role: "ADMIN" }];
            localStorage.setItem('users', JSON.stringify(initialUsers));
        }

        // ERROR HANDLING FUNCTIONS
        function showError() {
            const errorBox = document.getElementById('login-error');
            const loginCard = document.getElementById('login-card');
            
            errorBox.style.display = 'block';
            loginCard.classList.add('shake');
            
            // Remove shake class after animation completes so it can be re-triggered
            setTimeout(() => {
                loginCard.classList.remove('shake');
            }, 500);
        }

        function hideError() {
            document.getElementById('login-error').style.display = 'none';
        }

        const auth = {
            login: () => {
                const email = document.getElementById('login-email').value;
                const pass = document.getElementById('login-pass').value;
                const users = JSON.parse(localStorage.getItem('users'));
                
                const user = users.find(u => u.email === email && u.pass === pass);
                
                if (user) {
                    sessionStorage.setItem('loggedInUser', JSON.stringify(user));
                    initApp();
                } else {
                    showError(); // Trigger Warning Message
                    document.getElementById('login-pass').value = ''; // Clear password field
                }
            },
            register: () => {
                const name = document.getElementById('reg-name').value;
                const email = document.getElementById('reg-email').value;
                const pass = document.getElementById('reg-pass').value;
                if (!name || !email || !pass) return alert("Please fill all fields");

                const users = JSON.parse(localStorage.getItem('users'));
                users.push({ name, email, pass, role: "MEMBER" });
                localStorage.setItem('users', JSON.stringify(users));
                
                alert("Account created! You can now login.");
                toggleAuth('login-card');
            },
            logout: () => {
                sessionStorage.removeItem('loggedInUser');
                location.reload();
            }
        };

        function toggleAuth(id) {
            hideError();
            document.querySelectorAll('.auth-card').forEach(c => c.style.display = 'none');
            document.getElementById(id).style.display = 'block';
        }

        function initApp() {
            const user = JSON.parse(sessionStorage.getItem('loggedInUser'));
            if (!user) return;

            document.getElementById('auth-section').style.display = 'none';
            document.getElementById('app-shell').style.display = 'grid';
            document.getElementById('welcome-msg').innerText = `Welcome, ${user.name}`;
            document.getElementById('user-role-badge').innerText = user.role;

            const menu = document.getElementById('dynamic-menu');
            menu.innerHTML = '';

            const links = user.role === 'ADMIN' 
                ? [{ name: 'Dashboard', id: 'dash-page' }, { name: 'Members', id: 'admin-members-page' }, { name: 'Events', id: 'events-page' }]
                : [{ name: 'Dashboard', id: 'dash-page' }, { name: 'Events', id: 'events-page' }];

            links.forEach((link, idx) => {
                const div = document.createElement('div');
                div.className = `nav-item ${idx === 0 ? 'active' : ''}`;
                div.innerText = link.name;
                div.onclick = () => showPage(link.id, div);
                menu.appendChild(div);
            });
        }

        function showPage(pageId, el) {
            document.querySelectorAll('.page').forEach(p => p.classList.remove('active'));
            document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active'));
            document.getElementById(pageId).classList.add('active');
            el.classList.add('active');
        }

        window.onload = () => { if (sessionStorage.getItem('loggedInUser')) initApp(); };
    </script>
</body>
</html>
